# Web browser files
*.html

Wait, that would ignore all HTML files including the one that was modified. Since only a single HTML file was modified and no build artifacts, dependencies, or temporary files were detected, no additional ignores are needed.

(No output)